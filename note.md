The first time you set this repo up on a new computer:

```
git remote add upstream git@github.com:bbokser/bbokser.github.io.git
```

Whenever you want to update the website from here:
```
git push upstream main
```